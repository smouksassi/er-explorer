{
  "name": "@er-explorer/demo",
  "private": true,
  "version": "0.0.1",
  "description": "Minimal interactive exposure-response demo app for ER Explorer, built on the analysis, renderer, and session engines.",
  "scripts": {
    "build": "node scripts/build-data.mjs && node scripts/build.mjs",
    "clean": "rimraf dist src/data.generated.ts",
    "smoke:ui": "node scripts/ui-smoke.mjs"
  },
  "dependencies": {
    "@er-explorer/analysis": "workspace:*",
    "@er-explorer/data": "workspace:*",
    "@er-explorer/domain": "workspace:*",
    "@er-explorer/model-linear": "workspace:*",
    "@er-explorer/renderer": "workspace:*",
    "@er-explorer/session-engine": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "playwright": "1.49.1"
  },
  "license": "MIT"
}
