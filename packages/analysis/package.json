{
  "name": "@er-explorer/analysis",
  "version": "0.0.1",
  "private": false,
  "description": "Analysis engine for ER Explorer: the AnalysisModel/PredictionSurface/Diagnostic/ConfidenceInterval/ModelRegistry plugin architecture. Statistics are an implementation detail of future model plugins (Logistic, Linear, Emax, Ordinal, Kaplan-Meier, Cox, Clinical Utility), not of this package.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    "import": "./dist/index.js",
    "require": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm typecheck && vitest run",
    "lint": "eslint . --ext .ts",
    "clean": "rimraf dist tsconfig.build.tsbuildinfo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@er-explorer/domain": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  },
  "files": ["dist"],
  "keywords": ["er-explorer", "analysis", "statistical", "exposure-response", "plugins"],
  "license": "MIT"
}
