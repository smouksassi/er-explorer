{
  "name": "@er-explorer/renderer",
  "version": "0.0.1",
  "private": false,
  "description": "Framework-agnostic rendering engine for ER Explorer: composes Layers (Axis, Grid, Scatter, Fit, ConfidenceRibbon, ObservedStat, Distribution, Annotation) into publication-quality SVG (and, eventually, Canvas) visualizations. Pure rendering - no statistics, no model fitting, no React.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    "import": "./dist/index.js",
    "require": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm typecheck && vitest run",
    "lint": "eslint . --ext .ts",
    "clean": "rimraf dist tsconfig.build.tsbuildinfo tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@er-explorer/domain": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  },
  "files": ["dist"],
  "keywords": ["er-explorer", "renderer", "svg", "visualization"],
  "license": "MIT"
}
