export const createModelDefinition = (id, kind, description) => ({
    id,
    kind,
    description
});
export const createPredictionRequest = (exposures, covariates) => ({
    exposures,
    covariates
});
//# sourceMappingURL=index.js.map