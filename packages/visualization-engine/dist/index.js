export const createVisualizationSpec = (id, model, data, options) => ({
    id,
    model,
    data,
    options
});
export const createRenderResult = (outputType, content) => ({
    outputType,
    content,
    metadata: {}
});
//# sourceMappingURL=index.js.map